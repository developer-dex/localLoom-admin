/**
 * Public API for the reserved users Feature_Module (Requirements 10.1,
 * 12.2). Only the placeholder page is exposed until `features.users` is
 * flipped on and the full module (repository, hooks, schema, types) is
 * implemented per Requirement 10.4.
 */

export { default as UsersPage } from "./pages/users-page";
