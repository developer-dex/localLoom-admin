/**
 * Public API for the reserved reports Feature_Module (Requirements 10.1,
 * 12.2). Only the placeholder page is exposed until `features.reports` is
 * flipped on and the full module (repository, hooks, schema, types) is
 * implemented per Requirement 10.4.
 */

export { default as ReportsPage } from "./pages/reports-page";
